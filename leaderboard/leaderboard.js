  PlayersList = new Mongo.Collection('players'); //collection mise dans une variable pour la référencer
  //il n'y a pas var car c'est une variable globale


//le code placé dans les if est déplacé dans des dossiers client et sereurs 
//car tout ce qui est placé dans ces dossiers sera systematiquement executés par 
//respectivement le client et le serveur
//   if(Meteor.isClient){


//  }


//  if (Meteor.isServer) {

// }

